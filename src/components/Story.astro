---
import { Image } from "astro:assets";
import heroImage from "../assets/wedshots/leonardo-miranda-unsplash.jpg";

const story = `
We met on a crisp autumn afternoon, when a simple conversation blossomed into an inseparable friendship.
Over time, that friendship grew into love, and together we've shared adventures, dreams, and challenges.
Today, we celebrate not only our love, but also the family and friends who have joined us on this journey.
`;

const imageAlt = "Our story together";
---

<section
    class="py-24 px-6"
    style="background-color: var(--color-background); color: var(--color-secondary);"
>
    <div
        class="mx-auto flex max-w-6xl flex-col gap-12 md:flex-row md:items-center"
    >
        <div class="md:w-7/12 space-y-6">
            <p
                class="text-sm uppercase tracking-[0.35em] font-(--font-sans)"
                style="color: var(--color-primary); opacity: 0.7;"
            >
                Chapter One
            </p>
            <h2
                class="text-4xl sm:text-5xl tracking-tight font-(--font-display)"
                style="color: var(--color-primary);"
            >
                Our Story
            </h2>
            <p
                class="text-lg leading-relaxed whitespace-pre-line font-(--font-sans)"
                style="color: var(--color-primary); opacity: 0.8;"
            >
                {story}
            </p>

            <div
                class="mt-6 flex flex-wrap gap-3 text-sm font-(--font-sans)"
                style="color: var(--color-primary); opacity: 0.7;"
            >
                <span
                    class="inline-flex items-center gap-2 rounded-full border px-4 py-2"
                    style="border-color: var(--color-text-dark);"
                >
                    💌 From friends to soulmates
                </span>
                <span
                    class="inline-flex items-center gap-2 rounded-full border px-4 py-2"
                    style="border-color: var(--color-text-dark);"
                >
                    ✈️ Adventures across continents
                </span>
                <span
                    class="inline-flex items-center gap-2 rounded-full border px-4 py-2"
                    style="border-color: var(--color-text-dark);"
                >
                    💍 Engaged under autumn skies
                </span>
            </div>
        </div>

        <div class="md:w-5/12">
            <div class="relative">
                <div class="relative rounded-4xl border p-4 backdrop-blur">
                    <Image
                        src={heroImage}
                        alt={imageAlt}
                        class="h-full w-full rounded-3xl object-cover"
                        loading="lazy"
                        format="webp"
                    />
                </div>
            </div>
        </div>
    </div>
</section>
