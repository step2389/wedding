---
const title = "RSVP";
const subtitle = "Facci sapere se verrai al nostro matrimonio";
---

2bd3aa52-27fe-48ec-9180-a2679432af07

<section id="rsvp" class="py-16 px-4 bg-[#f0e4d6]">
  <div class="max-w-xl mx-auto border border-[#b89c86] rounded-[2rem] px-6 py-8
              bg-[#f5e9db] shadow-sm">
    <h2 class="text-center text-xl tracking-[0.2em] uppercase text-[#9a7f68] mb-4">
      RSVP
    </h2>
    <p class="text-center text-[15px] text-[#6c645b] mb-8">
      Facci sapere se sarai con noi nel nostro giorno speciale.
    </p>

    <!-- FORM -->
    <form
      action="https://api.web3forms.com/submit"
      method="POST"
      class="space-y-4 text-[15px] text-[#3c3a35]"
    >
      <input type="hidden" name="access_key" value="LA_TUA_ACCESS_KEY" />
      <input type="hidden" name="subject" value="Nuovo RSVP matrimonio" />
      <input type="hidden" name="from_name" value="Wedding Site" />

      <div>
        <label class="block mb-1">Nome e cognome *</label>
        <input
          name="name"
          type="text"
          required
          class="w-full border border-[#c8b5a2] rounded-full px-4 py-2
                 bg-transparent placeholder:text-[#b9a89a] focus:outline-none
                 focus:border-[#8a6a51]"
        />
      </div>

      <div>
        <label class="block mb-1">Email</label>
        <input
          name="email"
          type="email"
          class="w-full border border-[#c8b5a2] rounded-full px-4 py-2
                 bg-transparent placeholder:text-[#b9a89a] focus:outline-none
                 focus:border-[#8a6a51]"
          placeholder="(opzionale)"
        />
      </div>

      <div class="grid grid-cols-2 gap-4">
        <div>
          <label class="block mb-1">Parteciperai? *</label>
          <select
            name="attending"
            class="w-full border border-[#c8b5a2] rounded-full px-4 py-2
                   bg-transparent focus:outline-none focus:border-[#8a6a51]"
            required
          >
            <option value="yes">Sì, ci sarò</option>
            <option value="no">Purtroppo no</option>
          </select>
        </div>

        <div>
          <label class="block mb-1">Numero persone *</label>
          <input
            name="guests"
            type="number"
            min="1"
            required
            class="w-full border border-[#c8b5a2] rounded-full px-4 py-2
                   bg-transparent placeholder:text-[#b9a89a] focus:outline-none
                   focus:border-[#8a6a51]"
          />
        </div>
      </div>

      <div>
        <label class="block mb-1">Allergie / Note (opzionale)</label>
        <textarea
          name="notes"
          rows="3"
          class="w-full border border-[#c8b5a2] rounded-2xl px-4 py-2
                 bg-transparent placeholder:text-[#b9a89a] focus:outline-none
                 focus:border-[#8a6a51]"
        ></textarea>
      </div>

      <input
        type="hidden"
        name="redirect"
        value="https://wedding-anna-stefano.penz23.org/grazie"
      />

      <button
        type="submit"
        class="mt-4 w-full border border-[#b89c86] rounded-full py-3
               text-center text-[15px] tracking-[0.15em] uppercase
               text-[#3c3a35] bg-transparent hover:bg-[#d9c4af]
               transition-colors"
      >
        Invia conferma
      </button>
    </form>
  </div>
</section>